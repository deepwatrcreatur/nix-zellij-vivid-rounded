Status: `done`

Suggested branch: `fix/zellij-lib-cleanups`

## Progress

- verified `lib.nix` for duplicated or accidental config lines
- confirmed `border_enabled "false"` is used correctly in both bar generators

## Goal

Clean up duplicated or accidental config lines in `lib.nix`.

## Tasks

- remove duplicated `border_enabled "false"` and similar low-risk issues
